"use client";

import { LicenseInfo } from "@mui/x-license";

LicenseInfo.setLicenseKey(process.env.NEXT_PUBLIC_MUI_LISENCE_KEY as string);

const MuiLicense = () => {
  return null;
};

export default MuiLicense;
